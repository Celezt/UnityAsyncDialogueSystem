#title {
	height: 0px;
}

#node-border {
	min-width: 90px;
}

#contents > #top > #output {
	background-color: var(--dg-color-default);
}

.port-disable.port.output > #type {
	color: var(--dg-color-light);
}
